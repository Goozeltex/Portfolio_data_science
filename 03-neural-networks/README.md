# Neural Networks & Deep Learning

Full-connected networks (Million Songs), object detection (YOLO-like, ResNet50), generative models (WGAN, VAE, diffusion, flows), BERT summarization. PyTorch, torch_geometric.
